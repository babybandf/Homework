# 解题成果检查清单

> 我是专职检查员。每道题目完成后，按以下清单逐项检查。
> 发现问题立即修复，修复后**重新执行全部检查项**，直到全部通过。

---

## 一、解题逻辑（Markdown文档）

| # | 检查项 | 要点 |
|---|--------|------|
| 1 | 推理完备性 | 每一步有明确依据，不能有逻辑跳跃、猜想或未验证的步骤 |
| 2 | 分类讨论穷尽性 | 所有可能情况均已列出，不遗漏；无效情况需明确排除并说明原因 |
| 3 | 计算正确性 | 关键等式推导无误，最终答案经验证（代入检验或反证） |
| 4 | 方法合规性 | 解题方法不超纲，符合初中/高中对应年级知识范围 |
| 5 | 答案明确性 | 最终答案用加粗或引用块突出，表述清晰无歧义 |

## 二、几何插图（geometry_figures.py + PNG）

| # | 检查项 | 要点 |
|---|--------|------|
| 6 | 脚本可执行 | `python geometry_figures.py` 无报错，输出"All figures generated successfully!" |
| 7 | 图形正确性 | 顶点坐标、角度、边长比例与题意一致；标注内容与解题步骤对应 |
| 8 | 图形完整性 | 关键步骤均有配图；分类讨论每种情况有独立图或对比图 |
| 9 | 标注清晰度 | 标签无重叠、不被裁剪；角度弧线半径分层合理；颜色语义一致 |
| 9b | G-标准合规 | 直角符号/角弧/tick/标签偏移按 `L_ref` 归一（G1–G5）；线段标签贴近主线段（G5b）；密集点群标签方向分散（G7）；极密点群（<5%·L_ref）只标一个/淡显另一（G7b）；直角符号不套住别的标注点（G14）；zorder 分层符合 G8；auditor 按「创建→set_active→绘图→set_active(None)→check」顺序并返回空列表（G0/G11） |
| 9c | 步骤元素隔离 | 每张 `step*.png` 只强调当前步骤几何对象，前置上下文 `alpha ≤ 0.35` 淡显或省略（G10）；说明框放空白区（G12）；高亮不叠加穿点箭头（G13） |
| 9d | 几何线登记 | 三边、AO、垂线等**所有几何线**均通过 `draw_triangle(names=)`/`draw_ao`/`draw_aux_line` 登记；无裸 `ax.plot` 画的未登记几何线 |
| 10 | 图片引用有效 | Markdown中所有 `![...](...)` 指向实际存在的PNG文件 |
| 10b | 原题重绘 | 题目卡（step0）必须包含重新绘制的原题示意图，而非直接引用题图照片 |

## 三、网页展示（HTML）

| # | 检查项 | 要点 |
|---|--------|------|
| 11 | 页面可加载 | 本地HTTP服务器打开无白屏、无JS报错（检查浏览器控制台） |
| 12 | 公式渲染 | MathJax公式全部正确渲染，无原始LaTeX代码残留 |
| 13 | 图片/图形显示 | 所有PNG图片或JSXGraph交互图正常加载 |
| 14 | 步骤导航 | 卡片切换、进度指示器、前进/后退按钮功能正常 |
| 15 | 交互功能 | 提示问答可展开收起、键盘导航（←→）可用 |
| 16 | 响应式布局 | 移动端宽度（375px）下无溢出、文字可读 |
| 16b | 默认语言 | 打开页面默认显示中文（调用 setLang('zh')），非内联英文原文 |

## 四、内容一致性

| # | 检查项 | 要点 |
|---|--------|------|
| 17 | MD↔HTML同步 | 解题步骤、公式、配图、最终答案在两端完全一致 |
| 18 | 图文对应 | 文字描述的角度/边/点名称与插图中的标注一致 |
| 19 | 知识点归纳 | Markdown末尾包含知识点总结，HTML末尾有思路梳理页 |

---

## 执行流程

```
开始检查
  ├─ 逐项检查（一~四）
  ├─ 记录所有问题
  ├─ 逐一修复
  └─ 全部重新检查 → 通过 → 结束
```

---

## 自检报告模板（MUST，最终回复必须原样贴出）

> 此模板由 [AGENT.MD](AGENT.MD) §R6 强制要求。
> **每个**条目都 MUST 填写 `PASS` 或 `FAIL`（FAIL 必须附原因）。
> MUST NOT 用 "全部通过 / OK / ✓" 等概括语代替逐项填写。

```text
=== 解题任务自检报告 ===
题号: 0XX
目录: math/0XX/

[文件清单]
- [ ] math/0XX/0XX_solution.md
- [ ] math/0XX/0XX.html
- [ ] math/0XX/geometry_figures.py
- [ ] math/0XX/step*.png（列出全部，每个独立打勾）

[R1 知识范围]
  - 不超纲:                       PASS / FAIL（说明）

[R2 技术栈合规]
  - 使用 matplotlib 输出 PNG:     PASS / FAIL
  - 中文字体走 font/ 绝对路径:    PASS / FAIL
  - HTML 引入 Tailwind+MathJax:   PASS / FAIL
  - Markdown 公式为纯文本:        PASS / FAIL

[R3 文件齐全]
  - 四类文件全部存在:             PASS / FAIL
  - 无并列变体目录:               PASS / FAIL
  - 命名符合附表:                 PASS / FAIL
  - geometry_figures.py 可执行:   PASS / FAIL
  - LayoutAuditor 自检零违规:     PASS / FAIL（列出残余违规，若有）
  - G0 auditor 构造顺序正确（在绘图之前创建并 set_active）: PASS / FAIL
  - G-标准（视觉元素按 L_ref 归一）: PASS / FAIL
  - G5b 线段标签贴近主线 / 几何线均已登记: PASS / FAIL
  - G7b/G14 极密点群不双标 / 直角符号不套别点: PASS / FAIL
  - G12 说明框置空白区 / G13 高亮不叠箭头: PASS / FAIL
  - G10 步骤元素隔离（前置上下文淡显）: PASS / FAIL

[R4 网页组件齐全]
  - langZh / langEn 按钮:         PASS / FAIL
  - i18n.zh 与 i18n.en 字典完整: PASS / FAIL
  - 全部可见文本带 data-i18n:     PASS / FAIL（漏掉的 key 列表）
  - 步骤卡片 + goToStep:          PASS / FAIL
  - 进度指示器 + N/总数:          PASS / FAIL
  - 键盘 ←/→ 导航:                PASS / FAIL
  - 每步 ≥2 提示问答:             PASS / FAIL
  - 末尾思路梳理页:               PASS / FAIL
  - 页面默认语言为中文:           PASS / FAIL
  - 题目卡含重绘原题图:           PASS / FAIL

[R5 一致性]
  - MD↔HTML 题目/步骤/公式/答案: PASS / FAIL
  - 所有图片引用文件真实存在:     PASS / FAIL
  - data-i18n 键全部有翻译:       PASS / FAIL

[R7 模板溯源]
  - 从 templates/ 骨架复制构建:   PASS / FAIL

[结论]
  全部 PASS ? YES / NO
  若 NO，已修复并重新自检? YES / NO
=== 报告结束 ===
```

### 报告判定规则

- 必须 `[结论]` 行 = `YES` 且第二行 = `YES` 任务方可宣告完成。
- 任一 FAIL：MUST 立即修复，然后**重新执行整张清单**并贴出新报告，旧报告保留以体现修复轨迹。
- 缺少本报告 = 任务未完成。